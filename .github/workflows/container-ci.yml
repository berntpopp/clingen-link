name: Container CI

on:
  pull_request:
    paths:
      - clingen_link/**
      - docker/**
      - scripts/**
      - pyproject.toml
      - uv.lock
      - container-release.json
      - vendor/genefoundry/**
      - .github/workflows/container-ci.yml
      - .github/workflows/container-release.yml
      - .github/workflows/data-refresh.yml
  push:
    branches:
      - main
    paths:
      - clingen_link/**
      - docker/**
      - scripts/**
      - pyproject.toml
      - uv.lock
      - container-release.json
      - vendor/genefoundry/**
      - .github/workflows/container-ci.yml
      - .github/workflows/container-release.yml
      - .github/workflows/data-refresh.yml

concurrency:
  group: container-ci-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  container-ci:
    uses: berntpopp/genefoundry-router/.github/workflows/_container-ci.yml@c68eb8e1db9b75ca340f35a7ab37ae2265235a97 # genefoundry-router v0.9.2
